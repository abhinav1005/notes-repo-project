[
  {
    "entryId": "google-spanner",
    "type": "paper",
    "title": "Google Spanner Paper Notes",
    "tags": ["distributed-systems", "consistency", "databases", "google", "truetime"],
    "summary": "Notes on Google Spanner, the first globally distributed database with external consistency. Covers the TrueTime API, commit wait protocol, and how bounded clock skew enables strong guarantees across data centres.",
    "content": "## Overview\nGoogle Spanner is a globally distributed relational database that provides **external consistency** — a stronger guarantee than serialisability. It's the first system to distribute data globally while supporting general-purpose transactions and SQL queries.\n\n## TrueTime API\nThe key innovation is TrueTime, a globally synchronised clock API that exposes clock uncertainty as a first-class concept.\n\n| Method | Returns |\n|--------|----------|\n| `TT.now()` | `TTInterval [earliest, latest]` |\n| `TT.after(t)` | true if `t` has definitely passed |\n| `TT.before(t)` | true if `t` has definitely not passed |\n\nSpanner uses GPS receivers and atomic clocks in each data centre to keep uncertainty (ε) under **7ms** in practice.\n\n## Commit Wait\nTo achieve external consistency, every read-write transaction waits until `TT.after(commit_timestamp)` is true before releasing locks. This guarantees that any transaction that starts after the commit will observe its effects.\n\n```\ncommit_timestamp = TT.now().latest\nwait until TT.after(commit_timestamp)\nrelease locks\n```\n\n## Architecture\n\n```mermaid\ngraph TD\n  Client --> UniverseZone[Universe Master]\n  Client --> ZoneA[Zone A SpanServer]\n  Client --> ZoneB[Zone B SpanServer]\n  ZoneA --> Paxos[Paxos Group]\n  ZoneB --> Paxos\n  Paxos --> Colossus[Colossus Distributed FS]\n```\n\n## Key Takeaways\n- External consistency > serializability: a transaction that starts after another commits is guaranteed to see the newer state, even across data centres\n- TrueTime makes bounded clock skew an engineering problem, not a theoretical assumption\n- The 7ms commit wait is a real-world price paid for global consistency",
    "sourceUrl": "https://research.google/pubs/pub39966/",
    "sourceFile": "notes/google-spanner.md",
    "createdAt": "2026-02-25T00:00:00.000Z",
    "updatedAt": "2026-02-25T00:00:00.000Z"
  },
  {
    "entryId": "url-shortener",
    "type": "note",
    "title": "Designing a URL Shortener",
    "tags": ["system-design", "scalability", "databases", "caching", "redis"],
    "summary": "System design notes for a URL shortening service like bit.ly. Covers Base62 encoding for key generation, database schema, the redirect request flow, and scaling with Redis caching.",
    "content": "## Problem\nDesign a system that takes a long URL and returns a short alias (e.g. `bit.ly/xyz`). The system must redirect users from the short URL to the original one.\n\n## Core Design\n\nUse **Base62 encoding** (a-z, A-Z, 0-9) to generate a 7-character key, giving 62^7 ≈ 3.5 trillion possible URLs.\n\nTwo main approaches:\n1. **Hash-based**: MD5 the long URL, take the first 7 characters. Risk of collisions.\n2. **Counter-based**: Auto-increment an ID in the database, encode it in Base62. Guaranteed uniqueness.\n\n## Database Schema\n\n```sql\nCREATE TABLE urls (\n  short_code VARCHAR(10) PRIMARY KEY,\n  long_url   TEXT NOT NULL,\n  created_at TIMESTAMP DEFAULT NOW(),\n  click_count INT DEFAULT 0\n);\n```\n\n## Request Flow\n\n```mermaid\nsequenceDiagram\n  User->>API: POST /shorten { url: \"https://...\" }\n  API->>DB: INSERT short_code, long_url\n  API->>User: { short_url: \"https://short.ly/abc1234\" }\n  User->>API: GET /abc1234\n  API->>Cache: Lookup abc1234\n  Cache-->>API: Cache miss\n  API->>DB: SELECT long_url WHERE short_code = 'abc1234'\n  API->>Cache: SET abc1234 = long_url (TTL 24h)\n  API->>User: 301 Redirect to long_url\n```\n\n## Scaling Considerations\n- Cache hot URLs in **Redis** with LRU eviction — most traffic hits the same top ~20% of URLs\n- Use a **301 redirect** (permanent) to offload repeat lookups to the browser cache; use **302** if you need accurate click analytics\n- For very high write throughput, partition the counter across multiple nodes using a range-based ID allocator",
    "sourceUrl": "",
    "sourceFile": "notes/url-shortener.md",
    "createdAt": "2026-02-25T00:00:00.000Z",
    "updatedAt": "2026-02-25T00:00:00.000Z"
  }
]
